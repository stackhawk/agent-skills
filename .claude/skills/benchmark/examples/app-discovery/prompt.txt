Using the hawkscan skill, perform the app-understanding/discovery step ONLY for the repository in the current working directory. Do NOT run any security scan, do NOT install anything, and do NOT modify, create, or delete any files.

Determine, by investigating the repository:
1. How the app runs locally — the exact start command and the host/port it listens on.
2. Its API style — REST (note an OpenAPI/Swagger spec if present), GraphQL, gRPC, or a plain server-rendered web app — and the API base path.
3. Whether it is a single-page app (SPA) front end.
4. Whether exercising its endpoints requires authentication, and what login shape it appears to use.

End your response with a block that begins with the literal line "DISCOVERY:" followed by one line per answer:
run_command: ...
host: ...
api_style: ...
spa: yes|no
auth: required|not required — <shape>
